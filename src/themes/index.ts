import {colors as DefaultColors} from './default';
import {colors as DarkColors} from './dark';

const spacing = {
  sm: 12,
};

const fontSize = {
  sm: 12,
};

const theme = {
  spacing,
  fontSize,
  colors: {default: DefaultColors, dark: DarkColors},
};

export {theme};
